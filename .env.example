# ==============================================================================
# AIC Website — Environment Variables
#
# Copy this file to .env.local and fill in the values:
#   cp .env.example .env.local
#
# See docs/ARCHITECTURE.md for details on each integration.
# ==============================================================================

# --- Public (available in browser) -------------------------------------------

# Sanity CMS — create a project at https://sanity.io/manage
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01

# Site URL — used for CSP, preview links, and Open Graph metadata
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# --- Sanity (server-only) ----------------------------------------------------

# API read token — needed for draft mode (preview unpublished content)
# Create at: https://sanity.io/manage → API → Tokens → Add token (Viewer)
SANITY_API_READ_TOKEN=

# Write token — only needed for data migrations (scripts/migrate-to-sanity.ts)
# Create at: https://sanity.io/manage → API → Tokens → Add token (Editor)
SANITY_API_WRITE_TOKEN=

# Shared secret for draft mode entry (any random string)
SANITY_PREVIEW_SECRET=

# Shared secret for ISR webhook — must match the Sanity webhook config
SANITY_REVALIDATE_SECRET=

# --- Resend (email service) ---------------------------------------------------
# Create an account at https://resend.com

# API key from Resend dashboard
RESEND_API_KEY=

# Verified sender email (must be verified in Resend)
RESEND_FROM_EMAIL=

# Audience ID for newsletter subscribers (Resend → Audiences)
RESEND_AUDIENCE_ID=

# --- Form recipients ---------------------------------------------------------
# Email addresses that receive form submissions

CONTACT_FORM_TO_EMAIL=
FORM_TO_EMAIL=
SERVICE_INQUIRY_TO_EMAIL=
SUBSCRIBE_TO_EMAIL=

# --- YouTube ------------------------------------------------------------------
# YouTube Data API v3 — https://console.cloud.google.com/apis

YOUTUBE_API_KEY=
YOUTUBE_CHANNEL_ID=
